# プロジェクト

各プロダクトは、project 固有の collection、job、実装メモを `src/projects/<projectKey>` 配下に置きます。

shared platform concern は `src/core` 側に残します。

repo には `example` project を同梱しています。bootstrap の出力や project config / feature flag / billing note の置き場所を見るときの見本にしてください。
