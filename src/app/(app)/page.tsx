export default function HomePage() {
  return (
    <section>
      <p>Studio99 Application Core</p>
      <h1>Shared app, ops, and Payload surfaces are split by route group.</h1>
      <p>
        Start the product shell at <code>/app</code>, operate the platform at <code>/ops</code>,
        and manage content through <code>/admin</code>.
      </p>
    </section>
  )
}
