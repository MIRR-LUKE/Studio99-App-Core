export default function HomePage() {
  return (
    <section>
      <p>Studio99 Application Core</p>
      <h1>Frontend and Payload surfaces are wired in separate route groups.</h1>
      <p>
        This shell keeps the public app at <code>/</code> and Payload surfaces under{' '}
        <code>/admin</code> and <code>/api</code>.
      </p>
    </section>
  )
}
